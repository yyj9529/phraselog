// import { supabase } from "~/core/lib/supabase.client";
// import { Scene } from "./hooks/use-phrases";

// export const insertScene = async (scene: Scene) => {
//   const { data, error } = await supabase.from('scenes').insert(scene).select();
//   if (error) throw error;
//   return data;
// };
